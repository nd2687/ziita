<% @page_title = "下書き投稿一覧" %>

<h4><%= @page_title %></h4>

<% if @drafts.count == 0 %>
  こんにちは、<%= @current_user.identify_name %>さん<br>
  現在、<%= @current_user.identify_name %>さんの持つ下書き投稿はありません。
<% else %>
<ul class="list-group">
  <% @drafts.each do |draft| %>
    <li class="list-group-item article-wrap">
      <%= link_to draft.title, user_draft_path(identify_name: current_user.identify_name, access_token: draft.access_token) %>
    </li>
  <% end %>
</ul>
<% end %>
