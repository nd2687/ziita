<p><%= link_to '戻る', [ :user, :articles ] %></p>

<ul class="list-group">
  <% @comments.each do |comment| %>
    <li class="list-group-item">
      <p><%= comment.name %></p>
      <%= comment.body %>
      <p class="created">
        <%= comment.created_at.strftime('%Y/%m/%d %H:%M') %>
      </p>
    </li>
  <% end %>
</ul>
